name: 'Check stale issues/PRs.'
on: 
  schedule:
    # Run every hour on the hour
    - cron: '5 * * * *'

jobs:
    label-new-prs:
        uses: moorec-aws/.github/.github/workflows/reusable_stale_prs_and_issues.yml@mainline
